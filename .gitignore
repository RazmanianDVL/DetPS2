# Build
**/bin/
**/obj/
**/dist/
*.user
*.suo
.vs/

# User-supplied media (NEVER commit)
user-media.json
user-media.local.json
user-media-*.json
**/bios/
**/dumps/
**/isos/
*.BIN
*.bin
*.iso
*.ISO
*.cso
*.CSO
# allow source .bin references in docs only via explicit force if needed

# Local emulator state
**/memcard.ps2
**/*.mcd
**/*.dps2
**/*.inpr
detps2_frame.ppm
detps2_*.ppm
crash.log

# Ghidra-decompiled BIOS dumps (real Sony firmware, decompiled to pseudo-C/disasm text --
# copyrighted content even though it's text, not a binary; local diagnostics only, see
# docs/BIOS_DISSECTION.md's own "Do not commit BIOS blobs" note)
tools/bios-decomp/
tools/bios-extract/

# Scratch blocker-trace/investigation output logs (should live outside the repo, but in case
# they land here, never commit them)
mk-*.txt
d[0-9]*.txt
out/
out/traces/
# Root-level one-off investigation logs (use out/traces/ instead)
b3-*.txt
bo2-*.txt
gow-*.txt
sm-*.txt
dec-*.txt
*-err.txt
*-out.txt
v2-*.txt
snap-*.txt
*-final*.txt
*-score*.txt
*-menu*.txt
*-rpc*.txt

# OS
Thumbs.db
.DS_Store

# Local-only reference docs (machine-specific paths, not for the repo)
*.local.md

tools/b3-irx/

